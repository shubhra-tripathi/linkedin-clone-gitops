apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: linkedin-prod
spec:
  selector:
    app: messaging-service
  ports:
    - port: 8092
      targetPort: 8092
  type: ClusterIP