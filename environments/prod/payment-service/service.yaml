apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: linkedin-prod
  labels:
    app: payment-service
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8090
      targetPort: http
  type: ClusterIP
