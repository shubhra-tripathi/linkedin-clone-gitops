apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: linkedin-prod
spec:
  type: LoadBalancer
  selector:
    app: keycloak
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
