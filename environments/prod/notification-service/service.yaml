apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: linkedin-prod
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8090
      targetPort: http
  type: ClusterIP
