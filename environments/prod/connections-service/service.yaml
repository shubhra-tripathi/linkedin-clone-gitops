apiVersion: v1
kind: Service
metadata:
  name: connection-service
  namespace: linkedin-prod
spec:
  selector:
    app: connection-service
  ports:
    - name: http
      port: 8093
      targetPort: http
  type: ClusterIP
