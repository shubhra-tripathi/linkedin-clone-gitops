apiVersion: v1
kind: Service
metadata:
  name: feed-service
  namespace: linkedin-prod
  labels:
    app: feed-service
spec:
  selector:
    app: feed-service
  ports:
    - name: http
      port: 8087
      targetPort: http
  type: ClusterIP
