apiVersion: v1
kind: Service
metadata:
  name: userprofile-service
  namespace: linkedin-prod
  labels:
    app: userprofile-service
spec:
  selector:
    app: userprofile-service
  ports:
    - name: http
      port: 8083
      targetPort: http
  type: ClusterIP
