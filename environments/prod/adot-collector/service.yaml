apiVersion: v1
kind: Service
metadata:
  name: adot-collector
  namespace: linkedin-prod
spec:
  type: ClusterIP
  selector:
    app: adot-collector
  ports:
    - name: otlp-grpc
      port: 4317
      targetPort: otlp-grpc
    - name: otlp-http
      port: 4318
      targetPort: otlp-http